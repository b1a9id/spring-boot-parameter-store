spring:
  datasource:
    url: jdbc:mysql://localhost:33306/sample
    username: docker
    password: docker
cloud:
  aws:
    stack:
      auto: false
    region:
      auto: false
      static: ap-northeast-1
aws:
  paramstore:
    region: ${cloud.aws.region.static}
    name: sample
